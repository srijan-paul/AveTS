import { Type } from "./types";

// ! Incomplete and experimetnal type.
// ! The addition of this type to 
// ! the langauge at this point is uncertain

export default class IntersectionType extends Type {
    constructor(tag: string) {
        super(tag);
    }
}
